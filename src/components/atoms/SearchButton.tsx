import Icons from '../icons';

export default function SearchButton() {
  return (
    <button className='absolute right-0 bottom-0 h-full px-2 py-3 flex justify-center items-center'>
      <Icons.Logo />
    </button>
  );
}
